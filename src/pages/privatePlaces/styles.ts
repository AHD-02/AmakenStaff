import { Grid, styled } from "@mui/material";

export const TableIconContainer = styled(Grid)(() => ({
  backgroundColor: "#FBFBFB",
  borderRadius: "5px",
  justifyContent: "center",
  alignContent: "center",
}));
